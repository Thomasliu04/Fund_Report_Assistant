# 新一期简报出报指引

数据日当天：准备底稿 → 生成 **表图 Excel（主）** + PPT → 人工敲定观点句 / 按需调表美观。

**表图以底稿「终表」为准**（含他表 VLOOKUP 的排名列），格式对齐 25Q4 十页结构。

**推荐：网页操作台** `python main.py serve --open`

---

## 产物分工

| 产物 | 用途 |
|------|------|
| `output/xx_tables.xlsx` | **主产物**：各页终表 + 数据条/色阶/银华高亮 |
| `output/xx_briefing.pptx` | 叙事文字 + 表图（观点句需人工敲定） |

---

## 方式一：只导出表图 Excel（推荐先做）

```bash
python main.py export-tables \
  --xlsx "/path/to/25Q4简报底稿.xlsx" \
  --output output/25Q4_tables.xlsx \
  --period 25Q4 \
  --focus 银华
```

对照底稿抽查：总规模银华排名/增量、主动权益 Q 增量等。

---

## 方式二：网页

```bash
python main.py serve --open
```

上传底稿 → 确认日期 → 生成 → 分别下载 Excel / PPT（表图走终表）。

---

## 方式三：全册（终表灌图）

```bash
python main.py import-xlsx --xlsx "/path/to/底稿.xlsx" --out-dir data/25q4_from_draft

python main.py full-deck \
  --config config/report_25q4.yaml \
  --data data/25q4_from_draft \
  --draft-xlsx "/path/to/底稿.xlsx" \
  --output output/25Q4_briefing.pptx
```

---

## 底稿终表（固定 10 页）

| Sheet | 说明 |
|-------|------|
| `1整体情况` / `行业变化` | 第1页 |
| `2管理人总规模` / `总规模带格式` | 第2页（含 VLOOKUP 业务排名） |
| `3管理人非货` / `非货` | 第3页 |
| `4非货增量` / `非货增量排名` | 第4页 |
| `5主动权益` 等业务表 | 第5、7–10页 |
| `6权益ETF（含联接）` | 第6页双表：非货ETF + 权益ETF |

条件格式按**列**独立（数据条 / 色阶）。

---

## 建议流程

1. `export-tables` 出 Excel，与底稿终表对数字  
2. 在 Excel 里调美观后贴回 PPT（或再跑 full-deck）  
3. PPT 里改观点句；数字以终表为准，避免手抄
