# 新一期简报出报指引

数据日当天：准备底稿 → **校验底稿** → 生成 **表图 Excel（主）** + PPT → 人工敲定观点句 / 按需调表美观。

**表图以底稿「终表」为准**（含他表 VLOOKUP 的排名列），格式对齐 25Q4 十页结构。

**推荐：网页操作台** `python main.py serve --open`

> 安装与目录总览见 [`README.md`](README.md)。  
> 系统会长期沿用；底稿规范写在本文，出表前会自动跑健全性检查。改规则优先改 `briefing/importers/validate_draft.py` 顶部常量，并同步更新本节。  
> **需求背景与模块维护说明**见 [`docs/需求与维护说明.md`](docs/需求与维护说明.md)（含「列顺序是否影响结果」）。

---

## 产物分工

| 产物 | 用途 |
|------|------|
| `output/xx_tables.xlsx` | **主产物**：各页终表 + 数据条/色阶/示例高亮（人工贴进 PPT） |
| `output/xx_briefing.pptx` | **文字**：从终表读数自动成文（Q4 版式）；表图默认不贴，人工粘贴 |

---

## 期别口径（自动适配）

网页确认期别后，文字按三种模式成文（不必手改模板）：

| 模式 | 典型期别 | 主口径 | 辅口径 |
|------|----------|--------|--------|
| 单口径 | **Q1** | 仅单季（YTD≡季度，不写第二套） | — |
| 单季主导 | **Q3** | 第3季度 / Q3 | 前三季度（YTD）并列 |
| 期别主导 | **H1 / Q2、Q4** | 上半年或全年（`26H1` / `25年`） | 单季为附带 |

行业表日期列：常规 **3** 个 `YYYYMMDD`（期末→上季末→年初）；**Q1 可仅 2 列**（期末→年初）。

---

## 方式一：只导出表图 Excel（推荐先做）

```bash
# 建议先校验（不改文件）
python main.py validate-draft --xlsx "/path/to/底稿.xlsx"

python main.py export-tables \
  --xlsx "/path/to/底稿.xlsx" \
  --output output/26H1_tables.xlsx \
  --period 26H1 \
  --focus 示例
```

`export-tables` 会先打印底稿检查结果；若有 **error** 将中止导出（确认继续可加 `--force`）。

对照底稿抽查：总规模示例排名/增量、任意 1%/2% 增速、非货蓝条、ETF 左右表。

---

## 方式一附：生成 PPT 文字（读终表，不自动贴表）

```bash
python main.py full-deck \
  --config config/report_26h1.yaml \
  --data data/26h1_from_draft \
  --tables-xlsx output/26H1_tables.xlsx \
  --output output/26H1_briefing.pptx \
  --strict
```

或直接给底稿（会规范化后读表成文）：

```bash
python main.py full-deck \
  --config config/report_26h1.yaml \
  --data data/26h1_from_draft \
  --draft-xlsx "/path/to/底稿.xlsx" \
  --output output/26H1_briefing.pptx
```

默认使用 `25Q4_template` + `slide_map_q4`，**只灌文字**。需要系统贴表时加 `--paste-tables`（一般不推荐）。

成文后对照 `xx_tables.xlsx` 抽查示例排名/增速；缺公司或关键 sheet 会在终端打印读表 error，`--strict` 时中止。

---

## 方式二：网页

```bash
python main.py serve --open
```

上传底稿 → 确认日期 → 生成 → 分别下载 Excel / PPT（表图走终表）。

**有校验 error 时网页会禁止生成**（按钮灰掉；接口也返回 400）。请先改底稿再重新上传。warning 不拦。

生成后按下一节把 Excel 各 sheet 贴进 PPT 占位处。

---

## 终表 → PPT 粘贴清单（必做）

默认 **不自动贴表**；PPT 右侧是占位提示。打开 `output/xx_tables.xlsx` 的「使用说明」也可对照。

| PPT 页 | 终表 sheet | 粘贴位置 | 备注 |
|--------|------------|----------|------|
| 1 | `01_行业变化` | 右侧表图 | |
| 2 | `02_总规模` | 右侧表图 | |
| 3 | `03_非货` | 右侧表图 | |
| 4 | `04_非货增量` | 右侧表图 | |
| 5 | `05_主动权益` | 右侧表图 | |
| 6 | `06_非货ETF含联接` | **左侧** | 含联接 |
| 6 | `06_权益ETF含联接` | **右侧** | 含联接；**勿贴「不含联接」旧表** |
| 7 | `07_货币` | 右侧表图 | |
| 8 | `08_固收` | 右侧表图 | |
| 9 | `09_固收+` | 右侧表图 | |
| 10 | `10_FOF` | 右侧表图 | |

操作建议：在 Excel 选中数据区 → 复制 → 粘贴到 PPT 占位框（或先删占位再贴）。贴完后对照终表抽查示例排名/增速。

---

## 方式三：全册（终表灌图）

```bash
python main.py import-xlsx --xlsx "/path/to/底稿.xlsx" --out-dir data/26h1_from_draft

python main.py full-deck \
  --config config/report_26h1.yaml \
  --data data/26h1_from_draft \
  --draft-xlsx "/path/to/底稿.xlsx" \
  --output output/26H1_briefing.pptx
```

---

## 底稿终表规范（长期约定）

### 固定 10 页

| Sheet（推荐名） | 别名 | 说明 |
|-----------------|------|------|
| `1整体情况` | `行业变化` | 行业规模与增速 |
| `2管理人总规模` | `总规模带格式` / `总规模` | Top30 + 业务排名 |
| `3管理人非货` | `非货` | 含分类规模列 |
| `4非货增量` | `非货增量排名` | |
| `5主动权益` | `主动权益排名` | 业务表 |
| `6权益ETF（含联接）` | `ETF含联接` | 左非货ETF + 右权益ETF |
| `7货币` / `8固收` / `9固收+` / `10FOF` | 对应「…排名」别名 | 业务表 |

### 行业表（`1整体情况`）

1. 第 1 行含 **至少 2 个 `YYYYMMDD` 日期列**：  
   - 常规三列，顺序：**期末 → 上季末 → 年初**（例 26H1：`20260630` / `20260331` / `20251231`）  
   - **Q1 可两列**：**期末 → 年初**（例：`20260331` / `20251231`）
2. A1 建议写 **`类型`**（可空，系统会补）
3. 主品类标签须准确，建议连续、中间**不要空行**：  
   `合计 → 非货 → 货币 → 固收 → 被动权益 → 主动权益 → 固收+ → FOF`
4. REITs/另类可放后面；系统会跳过，不进入主品类表

### 排名 / 业务表

1. 第 1 行表头**连续无空**（空表头会截断后续列）
2. A 列为数字排名；公司列头为 `公司` / `基金公司`
3. 导出默认 **Top30**；至少准备 30 行更稳妥
4. 非货分类列：`主动权益`…`FOF`（可带「规模」后缀）→ 蓝色数据条

### ETF 表

1. 左表：从第 1 列起**连续表头**为非货 ETF（列数不固定；如 25Q3 可为 13 列含单季字段）
2. 右表起点表头须同时含 **`权益ETF` + `排名`**（位置可浮动，系统会定位；归一后写到 ≥第 20 列）

### 增速 / 增幅刻度（必读）

**同一列只能用一种写法：**

| 约定 | 单元格写法 | 显示 |
|------|------------|------|
| 百分数（H1 常用） | `2`、`53` | 2%、53% |
| Excel 小数（Q4 案例） | `0.02`、`0.53` | 2%、53% |

禁止同列混用 `0.06` 与 `2`，否则会出现 **1%→100%、2%→200%**。

系统按列中位数自动判断刻度。若整列已是百分数（中位数 ≥2），则 `0.84`、`-0.38` 视为很小的真实增速。疑似混用时只报 **warning**，**不拦生成**（可在 Excel 里再核对 1%/2% 显示）。

### 条件格式（由列名触发，无需手动画）

| 列名特征 | 效果 |
|----------|------|
| `规模` / `总规模` / `非货总计` / 非货分类规模 | 蓝色数据条 |
| `增量` / `排名变化` / `持营` / `净值变化` | 红色双向数据条 |
| 各类 `…排名` | 色阶 |
| `增速` / `增幅` | 百分比格式；行业表另加增速色阶 |

---

## 出报前验收清单（可勾选）

- [ ] 10 页 sheet 齐全（推荐名或别名）
- [ ] 行业表日期为 `YYYYMMDD`（常规 3 列；Q1 可 2 列），顺序正确，且与网页/config 一致
- [ ] 主品类 8 行齐全，中间无空行
- [ ] 各排名表有公司列、数字排名，约 Top30
- [ ] **增速列刻度统一**（全列百分数或全列小数）
- [ ] ETF 右表表头含「权益ETF」+「排名」
- [ ] `python main.py validate-draft --xlsx …` 无 error（网页有 error 时禁止生成）
- [ ] `export-tables` 后抽查：示例排名/增量、1%·2% 增速、规模蓝条、ETF 双表
- [ ] PPT 按「终表 → PPT 粘贴清单」贴完 10 页表图（第 6 页左右勿贴反）

---

## 建议流程

1. 复制上期正确终表为模板，只改数字与三个日期  
2. `validate-draft` → 修 error/warning  
3. `export-tables` 出 Excel，与底稿对数字  
4. 在 Excel 里调美观后贴回 PPT（或再跑 full-deck）  
5. PPT 里改观点句；**数字以终表为准，避免手抄**

---

## 自动规范化（系统会做，但仍建议底稿写对）

上传 / 导出时 `normalize_draft` 会：改名 sheet、补「类型」、截 Top30、ETF 右表挪到约第 20 列、部分表头别名归一。

**不会**替你重算规模/增速，也**不会**修好混用的增速刻度——混用必须改底稿。
